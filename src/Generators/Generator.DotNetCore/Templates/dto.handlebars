    public class {{ resolveDtoName | this }}
    {
    {{#each fields }}
        public {{{ resolveDtoName | type }}} {{ toPascal | name }} { get; set; }
    {{/each}}
    {{#each inputFields }}
        public {{{ resolveDtoName | type }}} {{ toPascal | name }} { get; set; }{{#if defaultValue }} = {{ defaultValue }};{{/if}}
    {{/each}}
    }
